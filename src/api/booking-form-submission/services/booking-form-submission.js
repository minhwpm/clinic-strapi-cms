'use strict';

/**
 * booking-form-submission service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::booking-form-submission.booking-form-submission');
